name: CI

on:
  release:
    types: [published]

# ← give GITHUB_TOKEN the right scopes
permissions:
  contents: write
  packages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # 1) Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.CR_PAT }}
          fetch-depth: 0

      # 2) Use the release tag
      - name: Compute image tag
        id: tag
        run: |
          # Remove 'v' prefix if present (e.g., v1.2.3 -> 1.2.3)
          TAG=${GITHUB_REF#refs/tags/}
          TAG=${TAG#v}
          echo "TAG=${TAG}" >> $GITHUB_OUTPUT

      # 3) Log in to GHCR
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: frallan97
          password: ${{ secrets.CR_PAT }}

      # 4) Build & push frontend
      - name: Build & push frontend image
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          push: true
          tags: ghcr.io/frallan97/hackaton-demo-frontend:${{ steps.tag.outputs.TAG }}

      # 5) Regenerate Swagger docs
      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.23'
      
      - name: Install swag
        run: |
          go install github.com/swaggo/swag/cmd/swag@latest
      
      - name: Regenerate Swagger docs
        run: |
          cd backend
          swag init -g main.go --output ./docs

      # 6) Build & push backend
      - name: Build & push backend image
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          push: true
          tags: ghcr.io/frallan97/hackaton-demo-backend:${{ steps.tag.outputs.TAG }}

      # 7) Bump Helm chart values
      - name: Bump Helm chart version in values.yaml
        run: |
          TAG=${{ steps.tag.outputs.TAG }}
          echo "Updating chart values to use tag $TAG"
          yq eval ".frontend.image.tag = \"${TAG}\"" -i charts/hackaton-demo/values.yaml
          yq eval ".backend.image.tag  = \"${TAG}\"" -i charts/hackaton-demo/values.yaml
          yq eval ".appVersion = \"${TAG}\"" -i charts/hackaton-demo/Chart.yaml

      # 8) Commit & push chart bump
      - name: Commit and push chart bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Switch to main branch and pull latest changes
          git checkout main
          git pull origin main
          # Add and commit the chart changes
          git add charts/hackaton-demo/values.yaml charts/hackaton-demo/Chart.yaml
          git commit -m "ci: bump chart to ${{ steps.tag.outputs.TAG }} [skip ci]"
          # Push to main branch
          git push origin main

      # 9) (Optional) Notify or output
      - name: CI complete
        run: echo "Images pushed and chart bumped to ${{ steps.tag.outputs.TAG }}" 